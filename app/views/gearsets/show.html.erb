<div class="row align-items-center text-center">
  <div class="col-12 my-3">
    <h3><%= @set.job.ens %> / <%= @set.user.name %> (<%= link_to "@#{@set.user.nickname}", "https://twitter.com/#{@set.user.nickname}" %>)</h3>
  </div>
</div>
<div class="row" id="gearset">
  <div class="col-md-6">
    <div class="mx-auto" id="gearmenu">
      <div class="row">
        <% slots.each do |slot| %>
          <div class="col-3">
            <% if @set[slot.name["en"]].present? %>
              <%= image_tag "/icons/64/#{@set[slot.name["en"]]}.png", size: "64x64", data: { toggle: "tooltip", "original-title": Item.find_by(id: @set[slot.name["en"]]).description[lang] } %>
            <% else %>
              <%= image_tag "/icons/64/#{slot[:img]}.png", size: "64x64" %>
            <% end %>
          </div>
        <% end %>
      </div>
      <div class="py-4 text-center">
        <%= link_to t('share_this_set'),"https://twitter.com/intent/tweet?text=Checkparam｜#{@set.job[lang]}%20@#{@set.user.nickname}&url=https://checkparam.com/gearsets/#{@set.id.to_s}&hashtags=FF11,checkparam", {onclick: "window.open(this.href,'hoge','height=400,width=600');return false do"} %>
      </div>
    </div>
  </div>
  <div class="col-md-6 mt-2" id="show">
    <% slots.all.each do |slot| %>
      <div class="row align-items-center text-center">
        <div class="col-3 py-1">
          <%= image_tag "/icons/64/#{@set[slot.name["en"]] || slot[:img]}.png", size: "48x48" %>
        </div>
        <% if @set[slot.name["en"]].present? %>
          <%= link_to Item.find_by(id: @set[slot.name["en"]])[lang], URI.encode("https://google.com/search?q=FF11+\"#{Item.find_by(id: @set[slot.name["en"]])[lang]}\""), {target: "_blank"} %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>