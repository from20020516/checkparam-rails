version: "3.8"
services:
  checkparam:
    build: .
    tty: true
    working_dir: /var/www
    volumes:
      - ..:/var/www:cached
    environment:
      - BUNDLE_PATH=/var/www/vendor/bundle
      - EDITOR=code --wait
      - RAILS_LOG_TO_STDOUT=1
      - RAILS_SERVE_STATIC_FILES=1
  mysql:
    image: mariadb
    environment:
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_DATABASE: checkparam
    volumes:
      - mysql:/var/lib/mysql
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    environment:
      - PMA_HOST=mysql
    tmpfs: /sessions
    ports:
      - 8080:80
volumes:
  mysql:
